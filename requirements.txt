# ─── Core (required) ────────────────────── ──────────────────────
requests>=2.31.0          # HTTP REST API publisher
python-dotenv>=1.0.0      # load configuration from the .env file

# ─── Hardware sensors (only when RUN_MODE=hardware) ──────────
smbus2>=0.4.2             # I2C bus (BME280)
RPi.bme280>=0.2.4         # BME280 temp/hum/pressure
spidev>=3.6               # SPI bus for MCP3008 (MQ-2, MQ-135, soil, pressure, battery)
RPi.GPIO>=0.7.0           # GPIO (relay/siren)
minimalmodbus>=2.1.0      # RS485 anemometer (Modbus RTU)
pyserial>=3.5             # Serial (A7670E/SIM7670E AT commands + Modbus)

# ─── Development / testing ────────────────────────────────────
# No additional dependencies; mock_sensors.py only uses stdlib
